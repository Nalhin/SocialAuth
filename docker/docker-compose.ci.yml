version: '3.7'

services:
  db:
    container_name: SocialMediaDB
    image: postgres:latest
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_DB=${DB_TEST}

volumes:
  db:
